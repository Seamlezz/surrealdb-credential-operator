---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: surrealdbcredentials.surrealdb.seamlezz.com
spec:
  group: surrealdb.seamlezz.com
  names:
    kind: SurrealDBCredential
    listKind: SurrealDBCredentialList
    plural: surrealdbcredentials
    singular: surrealdbcredential
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SurrealDBCredential is the Schema for the surrealdbcredentials
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines desired state of SurrealDBCredential.
            properties:
              database:
                description: |-
                  Database is the SurrealDB database for database-level users.
                  Required when level is Database. Omit for namespace-level users.
                maxLength: 128
                type: string
              level:
                description: Level is the SurrealDB level where the user is defined.
                enum:
                - Namespace
                - Database
                type: string
              policyRef:
                description: PolicyRef references the same-namespace SurrealDBTenantPolicy
                  authorizing this request.
                properties:
                  name:
                    description: Name is the same-namespace SurrealDBTenantPolicy
                      name.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              roles:
                description: Roles is the non-empty set of SurrealDB roles to grant.
                items:
                  description: SurrealRole is a built-in SurrealDB system-user role.
                  enum:
                  - VIEWER
                  - EDITOR
                  - OWNER
                  type: string
                maxItems: 3
                minItems: 1
                type: array
                x-kubernetes-list-type: set
              rotation:
                description: Rotation configures optional scheduled password rotation.
                properties:
                  period:
                    description: Period is the optional scheduled rotation period.
                    type: string
                type: object
              targetSecret:
                description: TargetSecret describes the generated Secret. It is always
                  written to the same namespace as this CR.
                properties:
                  name:
                    description: Name is the name of the generated Kubernetes Secret
                      in the same namespace as the credential CR.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - level
            - policyRef
            - roles
            - targetSecret
            type: object
            x-kubernetes-validations:
            - message: database is required when level is Database and must be omitted
                or empty when level is Namespace
              rule: (self.level != 'Database' || (has(self.database) && self.database.size()
                > 0)) && (self.level != 'Namespace' || !has(self.database) || self.database.size()
                == 0)
          status:
            description: status defines observed state of SurrealDBCredential.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the credential state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              database:
                description: Database is the resolved SurrealDB database for database-level
                  credentials.
                type: string
              lastManualRotationTrigger:
                description: LastManualRotationTrigger records the last processed
                  rotate-at annotation value.
                type: string
              lastRotationTime:
                description: LastRotationTime is the last time the managed password
                  was generated or rotated.
                format: date-time
                type: string
              nextRotationTime:
                description: NextRotationTime is the next scheduled rotation time
                  when spec.rotation.period is set.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest metadata.generation
                  observed by the controller.
                format: int64
                type: integer
              providerRef:
                description: ProviderRef is the provider resolved via the tenant policy.
                properties:
                  name:
                    description: Name is the name of the cluster-scoped SurrealDBProvider.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              surrealNamespace:
                description: SurrealNamespace is the resolved SurrealDB namespace.
                type: string
              username:
                description: Username is the deterministic SurrealDB username managed
                  by this credential.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
